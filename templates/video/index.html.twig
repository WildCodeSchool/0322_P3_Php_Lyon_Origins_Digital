{% extends 'base.html.twig' %}

{% block title %}Galerie Vidéo{% endblock %}

{% block body %}

    <main class="thumbs container">
        <table class="table">
            <thead>
                <tr>
                    <th>Titre</th>
                    <th>Description</th>
                    <th>Date de publication</th>
                    <th>poster</th>
                </tr>
            </thead>
            <tbody>
                {% for video in videos %}
                    <tr>
                        <td>{{ video.title }}</td>
                        <td>{{ video.description }}</td>    
                        <td>{{ video.postDate|date('d/m/Y') }}</td>    
                        <td><a href="{{ path('video_show', { id: video.id }) }}">
                            <img src="{{ asset('build/images/' ~ video.posterUrl) }}" alt="" height="300px" width="400px">
                        </a></td>
                    </tr>
                    {% else %}
                        
                        <p>Il n'y a pas de vidéos</p>
                    
                {% endfor %}
            </tbody>
        </table>
    </main>

{% endblock %}
