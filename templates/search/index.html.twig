{% extends 'base.html.twig' %}

{% block title %}Recherche
{% endblock %}
{% block javascripts %}
	{{ parent() }}
	{{ encore_entry_script_tags('addFav') }}
    {{ encore_entry_script_tags('addLater') }}
    {{ encore_entry_script_tags('addLike') }}
    {{ encore_entry_script_tags('displayViewCount') }}
{% endblock %}

{% block body %}

	{{ include('shared/gallery/bt-gallery.html.twig', {
        title: 'Les videos ' ~ tag.name,
        subtitle: 'Il y a ' ~ tag.videos|length ~ ' resultats correspondants à votre recherche',
        count: 10,
        contents: tag.videos,
        id: tag.name
    }) }}

{% endblock %}
